<Window x:Class="SubnauticaLauncher.UI.DebugTelemetryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Launcher Debug Telemetry"
        Width="720"
        Height="420"
        Topmost="True"
        Background="#101418"
        Foreground="White"
        ResizeMode="CanResizeWithGrip">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="StateText"
                   Grid.Row="0"
                   Text="State: (waiting)"
                   FontSize="15"
                   Margin="0,0,0,6"/>

        <TextBlock x:Name="ExplosionTimeText"
                   Grid.Row="1"
                   Text="Explosion Time: n/a"
                   FontSize="14"
                   Margin="0,0,0,4"/>

        <TextBlock x:Name="PositionText"
                   Grid.Row="2"
                   Text="Position: n/a"
                   FontSize="14"
                   Margin="0,0,0,4"/>

        <TextBlock x:Name="ProcessText"
                   Grid.Row="3"
                   Text="Process: none"
                   FontSize="13"
                   Foreground="#FFD0D0D0"
                   Margin="0,0,0,8"/>

        <TextBox x:Name="EventsTextBox"
                 Grid.Row="4"
                 IsReadOnly="True"
                 TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Auto"
                 Background="#151C24"
                 BorderBrush="#335A6A"
                 BorderThickness="1"
                 Foreground="White"
                 FontFamily="{DynamicResource AppFontFamily}"
                 FontSize="12"/>
    </Grid>
</Window>


