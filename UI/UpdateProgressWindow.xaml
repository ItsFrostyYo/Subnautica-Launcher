<Window x:Class="SubnauticaLauncher.UI.UpdateProgressWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Updating Subnautica Launcher"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        Width="520"
        Height="230"
        WindowStartupLocation="CenterOwner"
        FontFamily="{DynamicResource AppFontFamily}">

    <Border CornerRadius="14"
            BorderBrush="#66FFFFFF"
            BorderThickness="1"
            Background="#EE0A2537"
            Padding="18">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center">
                <Image Source="pack://application:,,,/Assets/Icons/SNLIcon.png"
                       Width="20"
                       Height="20"
                       Margin="0,0,10,0"/>
                <TextBlock Text="Subnautica Launcher Update"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"/>
            </StackPanel>

            <TextBlock x:Name="DetectedUpdateText"
                       Grid.Row="1"
                       Margin="0,14,0,0"
                       FontSize="14"
                       Foreground="#D8F0FF"
                       Text="Checking for updates..."/>

            <TextBlock x:Name="StatusText"
                       Grid.Row="2"
                       Margin="0,8,0,0"
                       FontSize="13"
                       Foreground="#FFE7C077"
                       Text="Preparing..."/>

            <ProgressBar x:Name="UpdateProgressBar"
                         Grid.Row="3"
                         Margin="0,14,0,0"
                         Height="12"
                         Minimum="0"
                         Maximum="100"
                         Value="0"
                         IsIndeterminate="True"
                         Foreground="#3DB4FF"
                         Background="#3F000000"/>

            <TextBlock x:Name="PercentText"
                       Grid.Row="4"
                       Margin="0,10,0,0"
                       FontSize="12"
                       Foreground="#C7DCEB"
                       Text="Starting..."/>
        </Grid>
    </Border>
</Window>


