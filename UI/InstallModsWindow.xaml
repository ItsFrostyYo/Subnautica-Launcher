<Window x:Class="SubnauticaLauncher.UI.InstallModsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Install Mods"
        Width="980"
        Height="560"
        MinWidth="980"
        MinHeight="560"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="{DynamicResource AppFontFamily}"
        WindowStyle="None"
        AllowsTransparency="True">

    <Window.Resources>
        <ImageBrush x:Key="BackgroundBrush"
                    Stretch="UniformToFill"/>

        <Style TargetType="TextBlock" BasedOn="{StaticResource {x:Type TextBlock}}">
            <Setter Property="Foreground" Value="White"/>
        </Style>

        <Style x:Key="DialogListStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
        </Style>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome
            CaptionHeight="25"
            ResizeBorderThickness="6"
            CornerRadius="0"
            GlassFrameThickness="0"
            UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Grid>
        <Rectangle Fill="{StaticResource BackgroundBrush}">
            <Rectangle.Effect>
                <BlurEffect Radius="8"/>
            </Rectangle.Effect>
        </Rectangle>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0"
                  Background="#40000000"
                  MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Image Source="pack://application:,,,/Assets/Icons/SNLIcon.png"
                       Width="16"
                       Height="16"
                       Margin="12,0,8,0"
                       VerticalAlignment="Center"/>

                <TextBlock Grid.Column="1"
                           Text="Install Mods"
                           VerticalAlignment="Center"
                           FontSize="13"
                           Margin="0,3,0,0"
                           FontWeight="SemiBold"/>

                <Button Grid.Column="2"
                        Content="&#x2715;"
                        Width="44"
                        Height="25"
                        Background="#8B2C2C"
                        BorderBrush="#55000000"
                        BorderThickness="1"
                        Click="Close_Click"
                        shell:WindowChrome.IsHitTestVisibleInChrome="True"/>
            </Grid>

            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Install and manage modded versions"
                           FontWeight="ExtraBold"
                           FontSize="20"
                           Margin="0,0,0,14"/>

                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="16"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0"
                            Background="#90000000"
                            CornerRadius="14"
                            Padding="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Install New Modded Version"
                                       FontWeight="Bold"
                                       FontSize="18"
                                       Margin="0,0,0,12"/>

                            <ComboBox x:Name="NewGameComboBox"
                                      Grid.Row="1"
                                      Style="{StaticResource MainDarkComboBox}"
                                      SelectionChanged="NewGameComboBox_SelectionChanged"
                                      Margin="0,0,0,10"/>

                            <ComboBox x:Name="NewModComboBox"
                                      Grid.Row="2"
                                      Style="{StaticResource MainDarkComboBox}"
                                      SelectionChanged="NewModComboBox_SelectionChanged"
                                      Margin="0,0,0,10"/>

                            <Border Grid.Row="3"
                                    Background="#66000000"
                                    CornerRadius="12"
                                    Padding="6">
                                <ListBox x:Name="NewVersionsList"
                                         Style="{StaticResource DialogListStyle}"
                                         SelectionChanged="NewVersionsList_SelectionChanged">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}"
                                                       Margin="6,4"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>

                            <Button x:Name="InstallNewModdedButton"
                                    Grid.Row="4"
                                    Content="Install"
                                    Width="120"
                                    HorizontalAlignment="Center"
                                    Margin="0,14,0,0"
                                    Background="#FF4FD38B"
                                    Style="{StaticResource RoundedButton}"
                                    Click="InstallNewModded_Click"/>
                        </Grid>
                    </Border>

                    <Border Grid.Column="2"
                            Background="#90000000"
                            CornerRadius="14"
                            Padding="16">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="Install Mod into Existing Version"
                                       FontWeight="Bold"
                                       FontSize="18"
                                       Margin="0,0,0,12"/>

                            <ComboBox x:Name="ExistingGameComboBox"
                                      Grid.Row="1"
                                      Style="{StaticResource MainDarkComboBox}"
                                      SelectionChanged="ExistingGameComboBox_SelectionChanged"
                                      Margin="0,0,0,10"/>

                            <ComboBox x:Name="ExistingModComboBox"
                                      Grid.Row="2"
                                      Style="{StaticResource MainDarkComboBox}"
                                      SelectionChanged="ExistingModComboBox_SelectionChanged"
                                      Margin="0,0,0,10"/>

                            <Border Grid.Row="3"
                                    Background="#66000000"
                                    CornerRadius="12"
                                    Padding="6">
                                <ListBox x:Name="ExistingVersionsList"
                                         Style="{StaticResource DialogListStyle}"
                                         SelectionChanged="ExistingVersionsList_SelectionChanged">
                                    <ListBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding DisplayName}"
                                                       Margin="6,4"
                                                       TextTrimming="CharacterEllipsis"/>
                                        </DataTemplate>
                                    </ListBox.ItemTemplate>
                                </ListBox>
                            </Border>

                            <Button x:Name="InstallExistingModButton"
                                    Grid.Row="4"
                                    Content="Install"
                                    Width="120"
                                    HorizontalAlignment="Center"
                                    Margin="0,14,0,0"
                                    Background="#FF4FD38B"
                                    Style="{StaticResource RoundedButton}"
                                    Click="InstallExistingMod_Click"/>
                        </Grid>
                    </Border>
                </Grid>

                <Button Grid.Row="2"
                        Content="Cancel"
                        Width="120"
                        HorizontalAlignment="Center"
                        Margin="0,16,0,0"
                        Background="#8B2C2C"
                        Style="{StaticResource RoundedButton}"
                        Click="Cancel_Click"/>
            </Grid>
        </Grid>
    </Grid>
</Window>
